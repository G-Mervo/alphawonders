# ── mvacant.com ──
# HTTP reverse proxy — SSL terminated at CloudFront + ACM
server {
    listen 80;
    listen [::]:80;
    server_name mvacant.com www.mvacant.com;

    # Redirect www → bare domain
    if ($host = www.mvacant.com) {
        return 301 https://mvacant.com$request_uri;
    }

    location / {
        proxy_pass http://mvacant-app:8080;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
        proxy_read_timeout 300;
        proxy_connect_timeout 10;
    }
}
