# ── Template: Add a new domain ──
# 1. Copy this file:  cp _template.conf.example mydomain.conf
# 2. Replace DOMAIN, CONTAINER, PORT
# 3. Run initial cert:
#    docker compose -f docker-compose.infra.yml exec certbot \
#      certbot certonly --webroot -w /var/www/certbot -d DOMAIN -d www.DOMAIN
# 4. Reload nginx:
#    docker compose -f docker-compose.infra.yml exec nginx nginx -s reload
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name DOMAIN www.DOMAIN;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://DOMAIN$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name DOMAIN www.DOMAIN;
#
#     ssl_certificate     /etc/letsencrypt/live/DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/DOMAIN/privkey.pem;
#     include /etc/nginx/snippets/ssl-params.conf;
#
#     if ($host = www.DOMAIN) {
#         return 301 https://DOMAIN$request_uri;
#     }
#
#     location / {
#         proxy_pass http://CONTAINER:PORT;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
